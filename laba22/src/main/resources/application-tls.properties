# HTTPS (TLS). Keystore и пароль — из переменных окружения (или GitHub Secrets в CI).
server.port=8443
server.ssl.key-store=${SSL_KEY_STORE:classpath:keystore.p12}
server.ssl.key-store-password=${SSL_KEY_STORE_PASSWORD:changeit}
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=${SSL_KEY_ALIAS:bank-server}
